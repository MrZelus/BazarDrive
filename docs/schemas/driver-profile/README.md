# Driver profile schemas

Артефакты схемы и типов для экрана профиля водителя.

## Содержимое
- `driver_profile_screen.schema.json` — JSON-схема экрана.
- `driver_profile.types.ts` — TypeScript-типы для payload/состояний.
